# Cloudflare Pages — optional CLI deploy: npm run deploy:cloudflare
# Dashboard deploy: build command `npm run build`, output directory `dist`
name = "football-mbti-quiz"
compatibility_date = "2024-09-01"
pages_build_output_dir = "dist"
